Разберись, почему перестало работать копирование текста из переписки Codex TUI в буфер обмена компьютера, и исправь причину.

Окружение: клиентский хост — ПК на Windows, с него я подключаюсь через Chrome по HTTPS к удалённому Ubuntu-серверу alexserv с code-server 4.138.0. Рабочая папка code-server — /home/alex/projects. Не называй Ubuntu-сервер хостом: системный буфер, куда нужно скопировать текст, находится на Windows-ПК. Раньше копирование работало. Сейчас я выделяю текст ответа Codex мышью, нажимаю Ctrl+C, затем в строке ввода Codex нажимаю Ctrl+V — вставляется старое содержимое буфера Windows.

Уже проверено:
- Разрешение Clipboard для сайта в Chrome включено.
- В пользовательских настройках code-server `terminal.integrated.copyOnSelection` равно `true`.
- `terminal.integrated.rightClickBehavior` возвращено к исходному значению `paste`.
- Добавленный при предыдущих попытках файл `keybindings.json` удалён.
- `/raw on` не помог.
- `/copy` не подходит: он копирует только последний ответ целиком, а мне нужно копировать произвольный выделенный фрагмент.

Мне нужны обычные Ctrl+C и Ctrl+V. Не предлагай Ctrl+Insert, дополнительные сочетания, кнопки интерфейса, которых может не быть, или копирование только последнего ответа.

Проверь фактический путь копирования Codex TUI на Ubuntu → терминал code-server → HTTPS/браузер Chrome на Windows → системный буфер обмена Windows: активный терминал, применённые настройки и биндинги code-server, состояние Clipboard API в браузере и ошибки Chrome/code-server. Сравни текущее состояние с доступными локальными логами или историей настроек, чтобы выяснить, что изменилось с момента, когда копирование работало. Не делай вывод о состоянии Windows по настройкам Ubuntu-сервера.

После нахождения причины внеси минимальное обратимое исправление и проверь именно полный сценарий: выделить произвольный фрагмент старого ответа Codex, нажать Ctrl+C, перейти в composer, нажать Ctrl+V и убедиться, что вставился выделенный текст, а не старый буфер. Не закрывай текущий TUI и не перезапускай code-server без необходимости. Не меняй сторонние службы и не заявляй, что исправил проблему, если проверить запись в буфер хоста не удалось. Если браузерный буфер недоступен из твоего окружения, точно укажи, какой участок цепочки не удалось проверить и почему.
